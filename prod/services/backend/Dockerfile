# Задать родительский образ, из которого
# будет создаваться наш образ
FROM openjdk:8-jre

# Задать рабочую директорию при старте
WORKDIR /usr/local/app

# Скопировать в образ файлы и папки.
# Инструкция ADD делает тоже самое что COPY,
# но ещё может распаковывать локальные .tar файлы
ADD bank_api-0.0.1-SNAPSHOT.jar back.jar

# Указать стартовую команду с аргументами, которую нужно
# выполнить когда контейнер будет запущен из образа.
# Параметры могут быть переопределены
CMD java -jar ./back.jar
